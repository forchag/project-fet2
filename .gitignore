# Build outputs
chaincode/hrbac/hrbac
chaincode/hrbac-corrected/hrbac-corrected

# Python cache/test artifacts
__pycache__/
*.py[cod]
.pytest_cache/
.venv/

# Performance dry-run outputs
results/*.csv

# Node dependencies/build artifacts
node_modules/
dist/
# ...but the built Elsevier submission packages are deliverables.
!paper_iot/dist/
# The zips are the deliverable; the staging trees they are built
# from are reproducible with make_submission.sh.
paper_iot/dist/V*-column/

# LaTeX build artifacts (the PDF is kept; intermediates are not)
*.aux
*.log
*.blg
*.out
*.spl
*.toc
*.synctex.gz
*.abs
missfont.log

# V10 real-data ingestion staging area (raw uploads, extracted copies,
# working inventories) -- never committed; canonical copies live under
# paper_iot/V10/analysis/ once verified and mapped.
tmp/
